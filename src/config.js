// API Configuration
const config = {
    // For local development
    // API_BASE_URL: 'http://localhost:3000',
    
    // For Render deployment - replace with your actual Render backend URL
    API_BASE_URL: 'https://fsp-ln45.onrender.com',
    
    // For production, you can also use environment variables
    // API_BASE_URL: process.env.REACT_APP_API_URL || 'https://your-render-backend-name.onrender.com'
};

export default config;
